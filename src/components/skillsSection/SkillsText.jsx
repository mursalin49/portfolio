import SectionHeader from "../common/SectionHeader";
import { sectionHeaders } from "../../data/portfolioData";

const SkillsText = () => {
  return <SectionHeader {...sectionHeaders.skills} />;
};

export default SkillsText;

import SectionHeader from "../common/SectionHeader";
import { sectionHeaders } from "../../data/portfolioData";

const ExperienceText = () => {
  return <SectionHeader {...sectionHeaders.experience} />;
};

export default ExperienceText;
